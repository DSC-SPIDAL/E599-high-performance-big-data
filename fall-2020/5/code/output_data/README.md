In the preprocessing step the model gives frames of images and gray scaled form of input data as output. After prediction the model gives anomaly scores for each bunch of frames(10 consecutive frames). 
